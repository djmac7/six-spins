# Raw Kaggle CSVs — Basketball-Reference-derived; do NOT redistribute (§1). Pin a snapshot
# locally / via LFS, but keep them out of the public repo. Ship only derived ratings.
data/raw/*.csv

# intermediate build artifacts (regenerated by the pipeline)
data/work/

# node
node_modules/

# python
.venv/
__pycache__/
*.pyc

# determinism snapshot
data/goat-data.json.run1

# Raw NBA box-score dataset (kaggle: eoinamoore/historical-nba-data-and-player-box-scores)
# — pinned locally for the playoff/clutch signal; keep out of the repo.
data/raw2/
